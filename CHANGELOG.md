# 更新紀錄

## 1.7.0 - 2026-08-26

- 班級調整：原五年級四班改為六年級，班級代碼 501／502／503／508 改為 601／602／603／608，學生編號同步改為 6101–6130、6201–6230、6301–6330、6801–6830。四年級 402、403 不變。
- 舊資料自動轉換：載入時把班級、課程單元、資源年級與目前班級一併轉到新制並重新編號，`student.id` 也同步改為 `stu-6101` 形式（維持 id 等於 `stu-<編號>` 的一致性）。所有以 id 為外鍵的資料——成績、缺考標記、出席、出席稽核、轉班紀錄、點數、觀察與抽籤歷史——會一併改名，因此紀錄不會斷開。
- 轉班與回收筒還原時，編號改變也會連帶更新 `student.id` 與所有關聯紀錄；若新 id 已被回收筒中的學生占用，會自動加上後綴避免覆蓋。
- 新增回收筒：刪除學生改為軟刪除，成績、出席、點數與觀察紀錄全部保留，可隨時還原或選擇徹底刪除（徹底刪除才會永久移除）。還原時若原座號已被占用，會自動改到空號並告知。
- 新增停用：保留學生歷史但不列入班級名單與統計，工具列可切換「顯示停用學生」。
- 新增轉班：學生連同所有紀錄轉到其他班級，編號改為新班級編號，原編號記錄在轉班紀錄中。班級滿 30 人時可使用 31 號以後的加號座位，避免轉入卡死。
- 修正手機版固定頁首寬度超出畫面、導致右上角設定圖示被推到看不見的問題。
- Apps Script 升級為 2.5.0：Students 分頁新增 `deletedAt` 欄，新增 TransferLog 分頁。

## 1.6.0 - 2026-08-26

- 評量可以自行維護：點欄位標題編輯名稱、類型、日期、滿分與權重，另有複製與刪除；刪除會提示將一併清除的成績筆數。
- 權重總和不等於 100% 時於成績頁顯示提醒。
- 新增缺考標記：標記後未補分時，成績以「實際應考同學平均 −5 分」計算，並隨其他人成績變動自動重算；直接填入分數即視為補考，以填入值為準。標記缺考若已有分數會先確認再清除，避免標記與計分不一致。
- 新增成績匯入（CSV，以座號對應，一次一項評量）與匯入範本下載。分數欄填「缺考」可直接標記缺考。
- 匯入預覽：對應摘要、逐列比對（目前分數 vs 檔案分數）、狀態標記（新增／覆蓋／不變／缺考／略過／問題）、問題列可單獨排除、覆蓋筆數警示，確認後可「復原這次匯入」。
- 匯出成績會標示缺考與推算分數。
- Apps Script 升級為 2.4.0：Scores 分頁新增 `status` 欄以保存缺考標記，自動備份也會一併還原。

## 1.5.0 - 2026-08-25

- 新增「出席紀錄」頁：月曆檢視一眼看完整月，每格顯示當日出席率並以顏色分級；點任一天可展開全班名單直接補改。
- 補改過去日期會留下稽核紀錄（時間、日期、學生、前後狀態），該日標記「已調整」，可從「修改紀錄」查閱最近 100 筆。當天的即時點名不列入紀錄，避免正常作業把稽核淹掉。
- 當月統計：班級出席率、有紀錄的上課日、缺席與遲到人次、逐日出席率零基線橫條圖。
- 需關注名單：自動標出當月缺席達 3 次或連續缺席 2 次的學生。
- 個別學生當月統計表，可依缺席、遲到、出席率或座號排序。
- 可匯出當月出席明細 CSV（含逐日狀態與合計）。
- 月曆、統計與名單皆依目前班級隔離，並可跨月切換。
- Apps Script 升級為 2.3.0：新增 `installDailyBackup` 每日自動備份觸發器（伺服器端執行，不需開著網頁），最多保留 30 份、超過自動清理；新增 AttendanceLog 分頁；連線診斷會檢查觸發器是否已安裝。

## 1.4.1 - 2026-08-25

- 修正連線診斷在「有項目未通過」時整包結果被丟棄的問題：診斷與自我測試回傳 `ok:false` 屬正常回報，先前被共用呼叫層當成連線失敗，畫面只顯示「Apps Script 未回傳成功狀態」而看不到任何檢查項目。
- 真正缺少錯誤訊息時，改為附上 Apps Script 的原始回應，便於判斷問題。

## 1.4.0 - 2026-08-25

- 課程單元改為依班級各自獨立：六班各有自己的單元主題、課次與今日任務，切換班級即切換課程；編輯時會標明只影響目前班級。
- 修正今日課堂的「課次」與「今日任務」原本寫死在 HTML、編輯後不會更新的問題。
- 教學資料庫改為依年級分類：資源標示「通用／四年級／五年級」，預設只顯示目前年級加通用資源，可切換為「全部年級」；卡片顯示適用範圍標籤，並可隨時調整。
- 上傳檔案自動套用目前班級的年級；新增連結時可直接指定適用範圍。
- 舊資料遷移：1.3.0 以前的單一課程單元會複製給六班當起點，未標記年級的資源一律視為通用。
- 評量定義與獎品目錄維持六班共用（同一套評量標準較公平，獎品由教師實體準備）。
- Apps Script 升級為 2.2.0：新增 Lessons 分頁記錄各班課程單元，Resources 分頁加入 `grade` 欄，Docs 報告改讀取該班級的課程單元。

## 1.3.0 - 2026-08-25

- 新增「連線診斷」：唯讀診斷檢查 Apps Script 版本、10 個 Sheets 分頁欄位、Drive 資料夾與最新備份；寫入測試實際建立並自動刪除暫存分頁、Drive 檔案與 Google 文件。診斷結果可一鍵複製為純文字。
- Apps Script 升級為 2.1.0：新增 `diagnose` 與 `selfTest` action、`SCRIPT_VERSION` 版本標記，前端會在部署版本落後時明確提示重新部署。
- 同步範圍補齊：新增 Classes、RewardMenu、Resources 三個分頁，Metadata 增加班級數、學生數與指令碼版本。
- 伺服器端強制去識別化：Students 分頁移除 `name` 欄，寫入 Sheets／Drive 備份／Docs 前一律剝除 `name`、`fullName`、`realName`。
- Google 呼叫改用共用進入點：加入逾時中斷（一般 30 秒、同步與報告 60 秒），並把登入頁重導、HTTP 401/403/404、授權失敗與非 JSON 回應轉成可操作的中文錯誤訊息。
- 同步鎖改為 `tryLock`，同時同步時回報「另一項同步正在進行中」而不是逾時例外。
- 重寫《Google Workspace 串接指南》：加入授權範圍、部署選項建議、八步驟驗收流程與疑難排解對照表。

## 1.2.0 - 2026-08-25

- 新增 402、403、501、502、503、508 六個班級與側欄班級切換器，每班預設 30 位匿名學生。
- 學生編號依班級產生：4201–4230、4301–4330、5101–5130、5201–5230、5301–5330、5801–5830。
- 移除真實姓名輸入與顯示；舊版資料載入時會立即清除姓名並轉為匿名編號。
- 新增匿名 CSV 範本、學生資料匯入、表格直接刪除及成績／點數排序。
- 新增學生人數、班級平均、中位數與平均點數統計，以及前 10 名零基線比較圖。
- 獎勵、成績、抽籤分組、教室座位與報表皆依目前班級隔離。

## 1.1.0 - 2026-08-25

- 新增六種獎勵行為說明與快速加點：發表、秩序、友愛、打掃、筆記及實驗安全。
- 新增四個點數級距與八項獎品目錄，包含科學玩具、3D 列印與科學盲盒。
- 獎品卡可直接開啟兌換；點數不足會顯示差額，成功後自動扣點並留下流水帳。
- 保留既有自訂獎品，舊資料會自動補入新版預設獎品欄位與缺少品項。

## 1.0.1 - 2026-08-25

- 增加互動教室畫布高度，修正新興科技工作坊、第五組實驗桌與自然教具展示區重疊。
- 以桌面及手機尺寸確認三組相關元件的交疊面積皆為 0。

## 1.0.0 - 2026-08-25

- 新增實驗桌小組加點、課堂 QR Code 與不錄音的即時音量燈。
- Google 串接新增最新備份還原、8 MB 內教材上傳及個別學生 Docs 報告。
- 完成本機 JSON 備份下載／還原與 10 頁瀏覽器回歸測試。
- 加入 GitHub Pages 自動部署、正式 Open Graph 圖片網址與中英文分享頁。

## 0.2.0 - 2026-08-25

- 依教師提供的「自然教室一」配置圖新增互動座位圖。
- 完整配置 5 張六人實驗桌與 1–30 號座位位置。
- 加入教師角、魚菜共生系統、自然教具區、新興科技工作坊與展示區資訊。
- 座位可直接更新出席狀態、查看學習摘要及給予正向點數。
- 新增隱藏學生姓名的教室投影模式，並保留固定離開按鈕。

## 0.1.0 - 2026-08-25

- 建立多頁式教師工作台與自然課堂工具。
- 加入學生、點數、成績、課堂紀錄、教學資源與備份功能。
- 提供 Google Sheets、Docs、Drive 與 Sites 的 Apps Script 串接範本。
