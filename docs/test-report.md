# 測試報告

測試日期：2026-08-25  
版本：1.5.0

## 出席紀錄與自動備份（1.5.0）

以 402 班 8 月 8 天、7 月 1 天、501 班 1 天的種子資料實測：

- [x] 月曆呈現 9 個有紀錄的日期與各日出席率（93%／97%／100%），無紀錄日顯示「—」
- [x] 月統計正確：出席率 95.9%、9 個上課日、缺席 11 人次、遲到 4 人次
- [x] 補改 8/03 的 4217 由缺席改為到課後，當日出席率 93%→97%，日期標題出現「（已調整）」，月曆格出現調整圓點
- [x] 稽核紀錄寫入 `2026-08-03 stu-4217 absent→present`，「修改紀錄」視窗正確列出
- [x] 當天（8/25）的點名切換**不**產生稽核紀錄，也不標記「已調整」
- [x] 需關注名單正確列出 4217（缺席 8 次、連續 8 次）與 4205（缺席 3 次、連續 3 次）
- [x] 排序：依遲到次數排序後 4210（遲到 4 次）置頂
- [x] 跨月切換：上一個月正確顯示 7 月 1 個上課日
- [x] 班級隔離：切到 501 後只剩 8/05 與當日兩筆，402 的 8 天不出現
- [x] CSV 匯出：標題列含逐日日期與合計欄，30 位學生共 31 行，MIME 為 text/csv
- [x] 手機版 375×812：月曆、名單、趨勢圖與名單卡片皆無水平溢位

## 診斷回報路徑修正（1.4.1）

- [x] 以實機收到的回應（`ok:false` 且附完整 `checks`）重測：15 項檢查正確逐列呈現，未通過項目標紅
- [x] `ok:false` 且帶 `error` 欄位（如「不支援的 action」）：顯示該訊息
- [x] `ok:false` 且無 `error` 也無 `checks`：顯示 Apps Script 原始回應，不再是無資訊的提示

## 課程分班與資源分年級（1.4.0）

- [x] 六班各有獨立課程單元：修改 402 的主題／課次／任務後，切到 501 仍為原設定，`lessons` 共 6 筆
- [x] 編輯視窗標示「只會更新 402 班的課程單元，其他班級不受影響」
- [x] 今日課堂的課次與今日任務會隨班級更新（修正原本寫死於 HTML 的缺陷）
- [x] 資源分年級：以 501（五年級）新增的資源，切到 402（四年級）後不顯示；切「全部年級」才出現
- [x] 新增連結的適用範圍預設為目前班級年級，選項為 通用／四年級／五年級
- [x] 卡片「適用範圍」可改標，改為四年級後立即出現在 402 的清單
- [x] 舊資料遷移：1.2.0 格式（單一 `lesson`、資源無 `grade`、學生含姓名）載入後，課程複製給六班、資源標為通用、姓名仍被匿名化、舊 `lesson` 欄位移除
- [x] 手機版 375×812：資料庫工具列與資源卡片皆無水平溢位

## Google 真實環境驗證（1.3.0）

由使用者的 Google 帳號實際部署 `Code.gs` 2.1.0 並完成授權後執行。

### 連線診斷

- [x] 儲存並測試：連線成功，版本檢查回報 `2.1.0（與前端相符）`
- [x] 唯讀診斷：10 個分頁欄位、Drive 資料夾與最新備份皆通過
- [x] 寫入測試：Sheets 寫入與讀回、Drive 檔案建立與讀回、Google Docs 建立與搬移三項全數 PASS，測試產物自動刪除

### 資料落地

- [x] 立即同步：`Classes` 6 列、`Students` 180 列
- [x] `Students` 標題列為 `id, classId, number, seat, tags, note, active, createdAt`，**無 `name` 欄**，資料中無真實姓名
- [x] Drive 資料夾產生 `backup-YYYYMMDD-HHMMSS.json`
- [x] 從 Google 還原：資料回填後班級切換器仍為 6 班、各班 30 人
- [x] 班級與個別學生 Google Docs 報告：建立於指定資料夾，內容只含學生編號
- [x] 8 MB 以下教材檔案上傳 Drive

### 尚未驗證

- [ ] Google Sites 嵌入與學校網域權限

## 前端診斷路徑驗證（1.3.0）

以 stub 攔截 `fetch` 模擬 Apps Script 回應，逐一驗證錯誤處理：

- [x] 版本相符與版本落後（2.0.0 → 明確提示重新部署）
- [x] 分頁欄位不符、備份尚未建立等未通過項目正確標紅
- [x] 寫入測試三步驟含失敗步驟與耗時顯示
- [x] 導向 Google 登入頁、HTTP 403、HTTP 404、網路失敗皆轉為可操作的中文訊息
- [x] 診斷期間按鈕鎖定，結束後解鎖；未填網址時擋下並提示
- [x] 手機版 375×812：長錯誤訊息不造成整頁水平溢位（`scrollWidth == clientWidth`）

## 自動檢查

- [x] `npm run build`（Vite 7.3.6，10 個 HTML 頁面均輸出）
- [x] 68 個模組完成轉換，QR Code 套件可正常打包
- [x] `npm audit`：0 個已知漏洞
- [x] `git diff --check`：無空白或修補格式錯誤
- [x] 所有頁面使用共用 JavaScript 與 CSS，無 Vite 錯誤覆蓋
- [x] 1200×630 社群分享縮圖以固定 Pages 網址輸出

## 瀏覽器流程

- [x] 10 頁逐頁巡檢：皆有標題與主內容，無 Vite 錯誤覆蓋
- [x] 今日課堂：30 位示範學生顯示、出席切換與快速回饋
- [x] 教室配置：5 張實驗桌、30 個座位與 5 個可點選場域均正確渲染
- [x] 小組加點：第一組 6 位學生各新增 1 點，操作後視窗正常關閉
- [x] 工具：QR Code 產生 canvas 並啟用 PNG 下載
- [x] 工具：無麥克風權限時顯示拒絕提示，不會錄音或卡住頁面
- [x] 備份：JSON 真實下載成功，並以同一檔案完成還原
- [x] 個別報告：學生選單列出 30 人；未設定 Google 時正確阻止送出
- [x] 分享頁：中英文文案與正式網址均存在
- [x] 手機版：390×844 下工具頁與教室頁無整頁水平溢位；配置圖只在指定容器捲動
- [x] 教室配置修正：工作坊／第三組、工作坊／展示區、第五組／展示區的交疊面積皆為 0
- [x] 獎勵說明：6 種集點規則、4 個兌換級距及 8 項獎品均正確呈現
- [x] 快速加點：「友愛同學」規則卡正確帶入 +2 與 30 位學生選單
- [x] 點數不足：4 點兌換 100 點獎品時正確阻止，並顯示尚差 96 點
- [x] 成功兌換：14 點兌換 12 點獎品後餘額為 2，流水帳新增 -12 紀錄
- [x] 獎勵手機版：390×844 無整頁水平溢位，規則與獎品目錄皆切換為單欄
- [x] 六班資料：班級切換器列出 402、403、501、502、503、508；切換 503 後正確顯示 5301–5330 共 30 人
- [x] 匿名遷移：含真實姓名的 1.x 舊資料載入後，畫面與 localStorage 均立即改為匿名學生編號
- [x] CSV 匯入：刪除 4230 後以範本重新匯入成功；即使檔案含姓名欄，也只保存「學生 4230」
- [x] 線上刪除：表格刪除後，學生、成績、點數、出席與觀察紀錄同步清除
- [x] 排序與視覺化：點數／成績高低排序正確；比較圖顯示前 10 名、直接數值標籤、單位及零基線說明
- [x] 跨頁班級隔離：今日課堂、教室座位、獎勵、成績與報表均只讀取目前班級 30 人
- [x] 學生頁手機版：390×844 可操作，整頁水平捲動被裁切，資料表僅在表格容器內橫向捲動

## QA 修正紀錄

- 新增 QR Code、音量燈、實驗桌小組加點。
- 新增 Google 最新備份還原、Drive 單檔上傳與個別學生 Docs 報告。
- 加入 GitHub Pages 自動部署、固定 Open Graph 圖片網址及中英文分享頁。
- 全站匿名化：不輸入或顯示真實姓名，投影模式另外隱藏學生編號與個別狀態。
- 補上 Sheets 同步遺漏的 Classes、RewardMenu、Resources 分頁；移除 Students 的 `name` 欄並在伺服器端強制去識別化。
- Google 呼叫加入逾時中斷與可操作的錯誤訊息，並新增連線診斷與寫入自我測試。
